# RSS_Monitor 依赖
# 由 run.sh 自动安装，无需手动执行

feedparser>=6.0.11       # RSS/Atom 解析
requests>=2.31.0         # 普通网页抓取
beautifulsoup4>=4.12.0   # 正文提取
lxml>=5.0.0              # BS4 解析器
PyYAML>=6.0              # 读取 config.yaml
playwright>=1.40.0       # 微信公众号抓取（复用 wechat_collector 登录态）

# P1 阶段（去重）需要额外安装：
# scikit-learn>=1.4.0
