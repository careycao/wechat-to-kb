# ── 登录态与个人凭证（绝对不能进仓库）──────────────────────────
*_state.json
cookies.txt
xhs_user_id.txt
.env

# ── 个人数据文件 ──────────────────────────────────────────────
# urls.txt 是个人历史记录，开源版用 urls.txt.example 替代
urls.txt
wechat_collector/urls.txt
web_collector/urls.txt

# 本地知识库配置
common/kb_config.local.py

# 微信登录态
wechat_collector/wechat_state.json

# RSS 个人订阅配置
rss_daily/config.yaml

# RSS 抓取数据库与日报（用户运行时生成）
rss_daily/data/

# ── Python 构建产物 ───────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
*.egg
dist/
build/
.eggs/

# ── 虚拟环境 ─────────────────────────────────────────────────
.venv/
venv/
env/

# ── 知识库内容（用户数据，不进仓库）──────────────────────────
knowledge_base/

# ── macOS ─────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── 运行日志 / 调试输出（不进仓库）──────────────────────────────
logs/
*.log

# ── 备份文件（不进仓库）────────────────────────────────────────
*.bak

# ── 过程产物 / 导入报告（不进仓库）──────────────────────────────
youdao_import_report*.md
*import_report*.md

# ── Cursor/Claude 工作流文件（可选：避免污染开源仓库）───────────
.claude/

# ── 编辑器 ───────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
