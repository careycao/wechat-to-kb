# tools/ 本地文档导入依赖
# 用于 import_local_docs.py（支持 PDF / PPTX / DOCX）
markitdown[all]>=0.0.1a3
pypdf>=4.0.0
python-pptx>=0.6.23
python-docx>=1.1.0
jieba>=0.42.0
# LLM 价值评估：默认走本机 `claude` CLI（Claude Code 登录态，无需 API Key）
# anthropic SDK 仅在未装 Claude Code 时用 ANTHROPIC_API_KEY 作为回退
anthropic>=0.39.0
